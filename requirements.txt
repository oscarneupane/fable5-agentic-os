langchain>=1.0.0
langgraph>=1.0.0
langsmith>=0.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Provider package. langchain-openai also drives OpenAI-compatible providers
# like GLM/Zhipu (FABLE5_PROVIDER=glm). Swap/add others as needed:
#   langchain-anthropic    # for FABLE5_PROVIDER=anthropic
langchain-openai>=0.2.0

# Dev/test:
pytest>=8.0.0
